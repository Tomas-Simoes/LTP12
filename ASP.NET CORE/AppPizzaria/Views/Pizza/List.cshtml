@model PizzaListViewModel

<h1>@Model.CategoriaAtual</h1>
<h2>@ViewData["Data"]</h2>
<hr/>
<h3>Total dde Pizzas: @ViewData["Total"]</h3>
<hr/>
<div class="container">
   <div class="row">
@foreach(var pizza in Model.Pizzas)
        {
            <partial name="_PizzasResumo" for="@pizza"/>
        }
    </div>
 </div>
